# ==============================================================================
# 🚀 CORE ASGI WEB SERVER FRAMEWORK LAYER
# ==============================================================================
fastapi==0.111.0
uvicorn==0.30.1
watchfiles==0.21.0              # 🔄 High-performance file watcher engine for reliable hot-reloading
python-multipart==0.0.9         # 📤 Essential binary multi-part compiler for form-data file uploads

# ==============================================================================
# 🗄️ RELATIONAL DATABASE LAYER (Asynchronous SQL Alchemy Pool)
# ==============================================================================
sqlalchemy==2.0.31
asyncpg==0.29.0                 # 🔌 Asynchronous database engine driver for modern PostgreSQL connections
aiosqlite==0.20.0               # 💾 👑 FIXED: Formal asynchronous driver for SQLite persistent storage loops

# ==============================================================================
# 🤖 AUTONOMOUS ORCHESTRATION & LLM INFERENCE CLIENTS
# ==============================================================================
groq==0.9.0                     # ⚡ Ultra-low latency engine to execute multi-agent cycles directly on Groq
chromadb==0.5.0                 # 💾 Local persistent document vector matrix storage index 
sentence-transformers==3.0.1    # 🌐 Local embedding generation algorithms for text representations
numpy==1.26.4                   # 📊 Matrix math layer for vectorized embedding comparisons

# ==============================================================================
# 📄 SECURE STRUCTURAL DOCUMENT EXTRACTION LAYER
# ==============================================================================
pypdf==4.2.0                    # 📖 Lightweight PDF text parsing library
pdfplumber==0.11.0              # 📐 High-fidelity tabular extractor to preserve structural text data alignments
opencv-python-headless==4.9.0.80

# ==============================================================================
# 🛡️ SYSTEM INTEGRITY, SECURITY SECURITY, & POLICY GUARDRAILS
# ==============================================================================
firebase-admin==6.5.0           # 🔑 Admin SDK integration to verify client authentication states
pyjwt==2.8.0                    # 🔒 Fallback security token encryption framework
bcrypt==4.1.3                   # 🔑 Secure cryptographic hashing utilities for credentials
cryptography==42.0.5            # 🛡️ Underlying transport cryptography engine
pydantic==2.9.2                 # 🏷️ High-speed parsing and state data models verification
pydantic-settings==2.3.4        # ⚙️ Environment variables loader boundary management
slowapi==0.1.9                  # 🛑 Global API route throttling engine to prevent endpoint abuse